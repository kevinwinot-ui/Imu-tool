// modules/ui/Turn34.js
// ---------------------------------------------------------------------------
// TURN 3–4 SCREEN (Variant C – Advanced IMU WebApp)
// ---------------------------------------------------------------------------
// Functionaliteit:
// • Toont turn-info (turn, DON, elders)
// • Advisor logic voor de stabilisatie-fase (turn 3–4)
// • Handweergave
// • Replay logging
// • Doorgaan naar Turn 5–7 (setup-phase)
// ---------------------------------------------------------------------------

import React from "https://esm.sh/react@18";
import { IMUState, goto } from "../state.js";
import { getAdvisorLine } from "../advisor.js";
import { Turnflow } from "../turnflow.js";
import { replayEvent } from "../replay.js";

export default function Turn34() {

  const advisor = getAdvisorLine();

  function next() {
    replayEvent("Einde Turn 3–4");
    Turnflow.next();  // navigeert automatisch → Turn57.js
  }

  return React.createElement(
    "div",
    {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "20px",
        padding: "20px",
        background: "#111",
        color: "white",
        borderRadius: "8px",
        height: "100%",
        overflowY: "auto"
      }
    },
    [

      // HEADER -------------------------------------------------------------
      React.createElement(
        "h2",
        { style: { fontSize: "24px", textAlign: "center" } },
        "Turn 3–4"
      ),

      // TURN INFO ----------------------------------------------------------
      React.createElement(
        "div",
        {
          style: {
            background: "#222",
            padding: "12px",
            borderRadius: "6px",
            fontSize: "14px",
            lineHeight: "1.5"
          }
        },
        [
          `Turn: ${IMUState.turn}`, React.createElement("br"),
          `DON: ${IMUState.don}`,  React.createElement("br"),
          `Elders: ${IMUState.elders}`
        ]
      ),

      // ACTIVE HAND --------------------------------------------------------
      React.createElement(
        "div",
        {
          style: {
            background: "#222",
            padding: "12px",
            borderRadius: "6px",
            fontSize: "14px",
            maxHeight: "200px",
            overflowY: "auto",
          }
        },
        [
          React.createElement("strong", { key: "h" }, "Hand:"),
          React.createElement("br", { key: "br1" }),
          ...IMUState.activeHand.map((c, idx) =>
            React.createElement(
              "div",
              { key: idx, style: { padding: "4px 0" } },
              `• ${c}`
            )
          )
        ]
      ),

      // ADVISOR ------------------------------------------------------------
      React.createElement(
        "div",
        {
          style: {
            background: "#003366",
            padding: "12px",
            borderRadius: "6px",
            fontSize: "15px",
            lineHeight: "1.4"
          }
        },
        [
          React.createElement(
            "div",
            { key: "t", style: { fontWeight: "bold", marginBottom: "6px" } },
            "🎯 Advisor:"
          ),
          advisor
        ]
      ),

      // NEXT BUTTON --------------------------------------------------------
      React.createElement(
        "button",
        {
          style: nextBtnStyle(),
          onClick: () => next()
        },
        "➡️ Ga naar Turn 5–7"
      ),

      // BACK BUTTON --------------------------------------------------------
      React.createElement(
        "button",
        {
          style: backBtnStyle(),
          onClick: () => goto("dashboard")
        },
        "⬅️ Terug naar Dashboard"
      )
    ]
  );
}

// ---------------------------------------------------------------------------
// STYLES
// ---------------------------------------------------------------------------

function nextBtnStyle() {
  return {
    padding: "14px",
    background: "#0066ff",
    color: "white",
    borderRadius: "6px",
    border: "none",
    cursor: "pointer",
    fontSize: "16px"
  };
}

function backBtnStyle() {
  return {
    padding: "12px",
    background: "#333",
    color: "white",
    borderRadius: "6px",
    border: "none",
    cursor: "pointer",
    fontSize: "16px",
    marginTop: "10px"
  };
}
